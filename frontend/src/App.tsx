/**
 * Application root.
 *
 * The route tree lands in E4 (T4.3.1: /, /assessment, /report) and E8 (/staff/login,
 * /staff/dashboard). E1 only establishes that the Vite + React + TypeScript skeleton
 * builds and mounts.
 */
export function App() {
  return (
    <main>
      <h1>English Assessment</h1>
      <p>Frontend skeleton — pages are implemented in E4 and E8.</p>
    </main>
  );
}
